spring.application.name=mindmap-backend
# Use environment variable for port, with a default
server.port=${PORT:5002}

# CORS configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}